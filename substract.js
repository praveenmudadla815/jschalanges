// --------------------first question---------------------------

const a = [1, 2, 3, 4, 5, 6];
const b = [6, 8, 9, 9, 8];

const subStra = a.map((value, index) => value - b[index]);

console.log(subStra);

// /------------------------------------ second question -----------------------

const a = [1, 2, 3, 4, 5, 6];
const b = [6, 8, 9, 9, 8];

const subStrat = b.map((value, index) => value - a[index]);

console.log(subStra);
